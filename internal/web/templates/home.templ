package templates

// Home is the template's placeholder landing page. Story #4 replaces the
// body with the real example feature vertical slice (HTMX + SSE).
templ Home() {
	@Layout("go-htmx") {
		<h1>go-htmx</h1>
		<p>Go + HTMX + SQLite production-ready template.</p>
	}
}
